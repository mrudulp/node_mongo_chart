version: "2"
services:
    web:
        build: .
        volumes: # Use this to mount app from local disk
            - ./:/usr/src/app
        ports:
            - "8080:8080"
        links:
            - mongo
    mongo:
        image: mongo
        ports:
            - "27017:27017"
        # volumes_from: 
        #     mongodata
    # mongodata:
    #     image: tianon/true
    #     volumes:
    #         - data/db
    #     command:
    #         echo 'Data container for Auth_MongoDb'